query getUserInteractions($input: ReadUserQueryInput!) {
  users(input: $input) {
    ...UserScalars
    comments {
      ...CommentWithPostEdge
    }
    reactions {
      ...ReactionWithAllEdges
    }
  }
}
